# Installer

aka **kernel installer**, aka **boot-patcher**, aka **boot image patcher**

This is based on [AnyKernel3](https://github.com/osm0sis/AnyKernel3).

```console
$ ./build.py --installer [...]
$ ./build.py [...]
```
